## 2025-05-14 - Search Accessibility Pattern
**Learning:** For a more accessible and power-user friendly search, implementing a global keyboard shortcut (like `/`) combined with a visible hint (using `<kbd>`) and a clear button makes discovery and usage much smoother.
**Action:** Apply this pattern to all filterable lists where a search input is present.
